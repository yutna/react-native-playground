import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 50,
  },
  bigBlue: {
    color: "blue",
    fontSize: 30,
    fontWeight: "bold",
  },
  red: {
    color: "red",
  },
});
